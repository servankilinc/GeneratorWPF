<UserControl x:Class="GeneratorWPF.View.Entrance"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GeneratorWPF.View"
             xmlns:vm="clr-namespace:GeneratorWPF.ViewModel"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="960">
    <!--<UserControl.DataContext>
        <vm:EntranceVM/>
    </UserControl.DataContext>-->
    <Grid>
        <Grid.RowDefinitions> 
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
 

        <StackPanel Grid.Row="1" Background="White" Margin="10">

            <StackPanel Margin="0, 0" Background="#dfdfdf">
                <Label FontSize="14" Margin="10">Select Project</Label>
            </StackPanel>

            <Border BorderBrush="#aaa" BorderThickness="0,0,0,1" />

            <DockPanel Grid.Column="0" Margin="0 0 0 20" HorizontalAlignment="Stretch" >
                <StackPanel Margin="10" Width="400">
                    <Label>Exist Projects:</Label>
                    <ComboBox 
                      ItemsSource="{Binding Projects, Mode=TwoWay}"
                      SelectedValue="{Binding SelectedProjectId, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                      SelectedValuePath="Id"
                      DisplayMemberPath="ProjectName"
                  />
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0 25 0 0">
                    <Button Content="Delete" Command="{Binding DeleteProjectCommand}" HorizontalAlignment="Right" Style="{StaticResource DangerBtnStyle}" />
                    <Button Content="Continue" Command="{Binding ContinueCommand}" HorizontalAlignment="Right" Style="{StaticResource SuccessBtnStyle}" />
                </StackPanel>
            </DockPanel>

            <Border BorderBrush="#aaa" BorderThickness="0,0,0,1" />

            <StackPanel Margin="0, 0" Background="#f5f5f5">
                <Label FontSize="14" Margin="10">Add New Project</Label>
            </StackPanel>
            <StackPanel Margin="10, 5">
                <Label Margin="0,10,0,0">Project Name:</Label>
                <TextBox Text="{Binding ProjectToCreate.ProjectName, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" Style="{StaticResource textBoxStyle}" />
            </StackPanel> 

            <Button Content="Save as a New Project" Command="{Binding AddNewProjectCommand}"  HorizontalAlignment="Right" Margin="15" Width="150" Style="{StaticResource PrimaryBtnStyle}" />

        </StackPanel>
    </Grid>
</UserControl>
